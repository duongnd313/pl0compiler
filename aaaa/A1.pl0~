Program TESTA1; 
Const MAX = 100, MIN = 0; 
VAR M, N, D, ADD;
  PROCEDURE ADD(VAR A);
  VAR M, D;
    PROCEDURE SUB;
    VAR D, M;
    BEGIN
	D := M + MAX;
    END;
  BEGIN
    M:=M+10;
  END;

Begin
  ADD();
End.

(* Test Khai bao don gian *) 
